# Used to validate webhook requests
REVALIDATE_SECRET=YOUR_SECRET_HERE

# Used to configure CORS, format links and more. No trailing slash
NEXT_PUBLIC_PAYLOAD_URL=http://localhost:3001

# Optional. reCAPTCHA v3 site key. Leave unset to run without reCAPTCHA:
# forms still submit, and the "protected by reCAPTCHA" notice stays hidden
# rather than claiming protection that isn't running.
# The matching RECAPTCHA_SECRET_KEY goes in apps/payload/.env
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=